dist/
node_modules/
.wrangler/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
_extract/
*.egg-info/

# Origin packages handed over by the author; unpacked into the tree above.
*.zip
conformance/out-check/
conformance/out/
conformance/fuzz-findings/
# The 1.0 fuzzer writes here; the pattern above predates it and did not
# cover it, so a run left untracked artifacts in `git status`.
conformance/fuzz-1.0-findings/
conformance/.fuzz-work/
repro-digest.txt
test_demo/_out/
rust/target/
# `python -m build` output.
python/build/
python/dist/
